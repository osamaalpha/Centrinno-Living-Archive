import styled from "styled-components";

export const NavigationBox = styled.nav`
  position: fixed;
  top: 0;
  right: 32px;
  z-index: 10;
  width: auto;
  display: flex;
  justify-content: flex-end;
  align-items: center;
`;
